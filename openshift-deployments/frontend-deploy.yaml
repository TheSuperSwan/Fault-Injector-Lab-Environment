apiVersion: apps/v1
kind: Deployment
metadata:
  name: file-frontend
spec:
  replicas: 1
  selector:
    matchLabels:
      app: file-frontend
  template:
    metadata:
      labels:
        app: file-frontend
    spec:
      containers:
        - name: file-frontend
          image: ghcr.io/thesuperswan/file-frontend:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 8080
          resources:
            requests:
              cpu: 25m
              memory: 64Mi
            limits:
              cpu: 200m
              memory: 128Mi
---
apiVersion: v1
kind: Service
metadata:
  name: file-frontend
spec:
  selector:
    app: file-frontend
  ports:
    - name: http
      port: 8080
      targetPort: 8080

